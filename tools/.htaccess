# UA: CLI-скрипти не для публічного доступу. EN: CLI tools are not public.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
